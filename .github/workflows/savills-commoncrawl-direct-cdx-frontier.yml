name: Savills Direct Common Crawl CDX Recovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Savills Direct Common Crawl CDX recovery is manual-only while production is stabilised."
