name: Savills Indexed Asset ID Recovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Savills indexed asset-ID recovery is manual-only while production is stabilised."
