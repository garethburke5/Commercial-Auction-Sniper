name: Savills PropertyAuctions Chronology Recovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Savills PropertyAuctions chronology recovery is manual-only while production is stabilised."
