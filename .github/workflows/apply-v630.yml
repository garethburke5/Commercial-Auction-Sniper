name: Apply V6.30 fast-scan grid

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_v630_scan_grid.py'
      - '.github/workflows/apply-v630.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply V6.30 fast-scan grid
        run: python scripts/apply_v630_scan_grid.py
      - name: Verify app
        run: python -m py_compile app.py
      - name: Commit UI repair
        run: |
          git config user.name 'Auction Sniper Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app.py
          git diff --cached --quiet && exit 0
          git commit -m 'Release V6.30 fast-scan grid'
          git pull --rebase origin main
          git push origin main
