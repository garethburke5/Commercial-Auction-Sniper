name: Auction Sniper Historical Backfill

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical backfill is temporarily manual-only while production deployment is stabilised."
