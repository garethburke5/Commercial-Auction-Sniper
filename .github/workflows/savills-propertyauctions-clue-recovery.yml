name: Savills Indexed Aggregator Clue Recovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Savills indexed aggregator clue recovery is manual-only while production is stabilised."
