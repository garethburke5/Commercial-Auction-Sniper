name: Apply V6.53 priority source boot hydration
on:
  push:
    paths:
      - 'scripts/apply_v653_priority_boot.py'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/apply_v653_priority_boot.py
      - run: python -m py_compile app.py
      - run: |
          git config user.name 'Auction Sniper Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app.py
          git diff --cached --quiet && exit 0
          git commit -m 'V6.53 hydrate priority sources on app load'
          git push origin main
