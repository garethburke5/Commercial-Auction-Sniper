name: Savills Common Crawl Frontier Recovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Savills frontier recovery is temporarily manual-only while production deployment is stabilised."
