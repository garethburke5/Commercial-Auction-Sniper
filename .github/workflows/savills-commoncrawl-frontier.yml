name: Savills Common Crawl Frontier Recovery

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/savills-commoncrawl-frontier.yml'
      - 'savills_manifest_commoncrawl_frontier_recovery.py'
      - 'savills_legacy_aid_capture_recovery.py'
      - 'savills_wayback_venue_frontier_recovery.py'
      - 'savills_public_search_frontier_recovery.py'
      - 'savills_legacy_pid_reference_frontier_recovery.py'
      - 'savills_public_address_frontier_recovery.py'
      - 'savills_history_integrity_repair.py'
  schedule:
    - cron: '41 * * * *'

permissions:
  contents: write

concurrency:
  group: auction-sniper-production-scan
  cancel-in-progress: false

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install playwright
          python -m playwright install chromium
      - name: Compile strict frontier recovery
        run: python -m py_compile savills_manifest_commoncrawl_frontier_recovery.py savills_legacy_aid_capture_recovery.py savills_wayback_venue_frontier_recovery.py savills_public_search_frontier_recovery.py savills_legacy_pid_reference_frontier_recovery.py savills_public_address_frontier_recovery.py savills_history_integrity_repair.py history_database.py collectors/savills.py
      - name: Enforce lot-specific canonical evidence
        run: python savills_history_integrity_repair.py
      - name: Recover oldest unresolved Savills manifest date from Common Crawl
        continue-on-error: true
        run: python savills_manifest_commoncrawl_frontier_recovery.py --max-capture-rows 180 --max-warc-checks 80 --max-live-checks 50
      - name: Recover frontier from Wayback PastAuctions and Venue bodies
        continue-on-error: true
        run: python savills_wayback_venue_frontier_recovery.py --max-captures 140 --max-live-checks 120
      - name: Recover frontier from public search indexes
        continue-on-error: true
        run: python savills_public_search_frontier_recovery.py --max-candidates 80
      - name: Mine public references for legacy Savills pid aid signatures
        run: python savills_legacy_pid_reference_frontier_recovery.py --max-result-pages 100 --max-candidate-checks 160
      - name: Validate strict Savills evidence and acceptable outcome
        run: |
          python - <<'PY'
          import json,re
          from pathlib import Path
          from urllib.parse import parse_qs,urlparse
          h=json.loads(Path('data/property_history.json').read_text())
          p=json.loads(Path('data/historical_backfill_progress.json').read_text())
          s=(p.get('sources') or {}).get('Savills Auctions') or {}
          events=[e for e in h.get('auction_events',[]) if e.get('source')=='Savills Auctions']
          def exact(url):
              q=urlparse(url or '')
              if q.scheme!='https' or (q.hostname or '').lower()!='auctions.savills.co.uk': return False
              low=(q.path or '').lower(); args=parse_qs(q.query)
              if ('lotdetails' in low or 'index.php' in low) and (args.get('pid') or (args.get('view')==['commission'] and args.get('id'))): return True
              return bool(re.search(r'/auctions/[^/?#]+-\d+/(?:[^/?#]+-)?\d+/?$',q.path or '',re.I))
          bad=[e for e in events if not exact((e.get('source_evidence') or {}).get('listing_url'))]
          assert not bad, f'{len(bad)} Savills events lack lot-specific evidence'
          assert s.get('historically_complete') is False
          pid=s.get('legacy_pid_reference_frontier_last_run') or {}
          assert pid, 'Legacy pid/aid reference route did not record an outcome'
          if not pid.get('canonical_events_added'):
              blocker=s.get('legacy_pid_reference_frontier_last_blocker') or {}
              assert blocker.get('frontier_date') and blocker.get('route') and blocker.get('next_safe_route'), 'Zero-event run missing concrete source-specific blocker and next route'
              assert s.get('next_frontier_repair') == 'savills_public_address_frontier_recovery.py', 'Next distinct repair was not persisted'
          print('SAVILLS_EVENTS',len(events))
          print('EARLIEST_VERIFIED',s.get('earliest_date_reached'))
          print('LEGACY_PID_REFERENCE_FRONTIER',pid)
          print('NEXT_FRONTIER_REPAIR',s.get('next_frontier_repair'))
          PY
      - name: Publish canonical history or exact blocker
        run: |
          git config user.name 'Auction Sniper History Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add data/property_history.json data/historical_backfill_progress.json data/source_diagnostics || true
          if ! git diff --cached --quiet; then
            git commit -m 'Advance Savills frontier through legacy pid references'
            for attempt in 1 2 3; do
              git fetch origin main
              if ! git rebase origin/main; then git rebase --abort || true; exit 1; fi
              if git push origin HEAD:main; then exit 0; fi
              sleep $((attempt * 3))
            done
            exit 1
          fi
